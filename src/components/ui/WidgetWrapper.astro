---
import type { HTMLTag } from 'astro/types';
import type { Widget } from '~/types';
import { twMerge } from 'tailwind-merge';

export interface Props extends Widget {
  containerClass?: string;
  ['as']?: HTMLTag;
}

const { id, containerClass = '', bg, as = 'section' } = Astro.props;

const WrapperTag = as;
---

<WrapperTag class="relative not-prose scroll-mt-header" {...id ? { id } : {}}>
  <div class="absolute inset-0 pointer-events-none -z-[1]" aria-hidden="true">
    <slot name="bg">{bg && <Fragment set:html={bg} />}</slot>
  </div>
  <div
    class={twMerge(
      'relative mx-auto max-w-7xl px-4 md:px-6 py-12 md:py-16 lg:py-20 text-foreground intersect-once intersect-quarter intersect-no-queue motion-safe:opacity-0 motion-safe:intersect:animate-fade',
      containerClass
    )}
  >
    <slot />
  </div>
</WrapperTag>
